<% if current_user.teacher %>
  <%= render 'questions/show_teacher/teacher', question: @question, answers: @answers %>
<% else %>
  <%= render 'questions/show_student/student', question: @question, answer: @answer, choice_array: @choice_array %>
<% end %>
