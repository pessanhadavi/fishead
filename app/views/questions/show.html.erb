<% if current_user.teacher %>
  <%= render 'questions/teacher', question: @question, answers: @answers %>
<% else %>
  <%= render 'questions/student', question: @question, answer: @answer, choice_array: @choice_array %>
<% end %>